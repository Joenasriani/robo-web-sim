export const SIMULATOR_CONFIG = {
  moveStep: 0.5,
  turnStep: Math.PI / 8,
  queueCommandDelay: 600,
  cameraPosition: [8, 8, 8] as [number, number, number],
  cameraTarget: [0, 0, 0] as [number, number, number],
};
